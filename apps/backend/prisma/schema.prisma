generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models added starting Milestone 1 (ACCOUNT, SIWE_SESSION) — see docs/07-database-design.md
